# Sandbox Rule Policy

This example packages a `sandbox-process` binary for `rule.evaluate/v1` and `config.validate/v1` using the Go `plugin/sandboxsdk` helper.

The first non-Go sandbox SDK example is future work; S10 ships the Go helper and examples first.
