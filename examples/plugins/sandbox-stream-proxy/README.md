# Sandbox Stream Proxy

This example packages a `sandbox-process` binary that registers `upstream.connect/v1` in `protocol-proxy` mode for the `stream.proxy/v1` relay contract.

The first non-Go sandbox SDK example is future work; S10 ships the Go helper and examples first.
