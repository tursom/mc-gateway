# Sandbox Stream Proxy

This example packages a `sandbox-process` binary that registers
`upstream.connect/v2` and fully handles the untouched client byte stream through
the sandbox takeover relay.

The first non-Go sandbox SDK example is future work; S10 ships the Go helper and examples first.
