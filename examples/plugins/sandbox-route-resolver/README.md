# Sandbox Route Resolver

This example packages a `sandbox-process` binary for `route.resolve/v1` and `config.validate/v1` using the Go `plugin/sandboxsdk` helper.

The first non-Go sandbox SDK example is future work; S10 ships the Go helper and examples first.
